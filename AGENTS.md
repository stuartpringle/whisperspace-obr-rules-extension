Always run `npm run build` at the end of each completed job unless the user explicitly says not to.
After each user-requested change set, create a git commit using Conventional Commits with scope, e.g. `feat(ui): ...`.
